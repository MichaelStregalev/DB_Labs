-- 1 SELECT COUNT(*) FROM PICTURES;
-- 2 SELECT COUNT(*) FROM USERS;
-- 3 SELECT COUNT(*) FROM ALBUMS;

-- 4
-- SELECT USERS.ID, USERS.NAME, COUNT(ALBUMS.ID) AS ALBUM_COUNT
-- FROM USERS
-- INNER JOIN ALBUMS ON USERS.ID = ALBUMS.USER_ID
-- GROUP BY USERS.ID
-- ORDER BY ALBUM_COUNT DESC
-- LIMIT 1;

-- 5
-- SELECT USERS.NAME, COUNT(TAGS.USER_ID) AS TAG_COUNT
-- FROM USERS
-- INNER JOIN TAGS ON USERS.ID = TAGS.USER_ID
-- GROUP BY USERS.ID, USERS.NAME
-- ORDER BY TAG_COUNT DESC
-- LIMIT 1;