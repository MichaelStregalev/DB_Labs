-- 1 SELECT ALBUMS.name, USERS.name from ALBUMS INNER JOIN USERS ON ALBUMS.USER_ID = USERS.ID;
-- 2 SELECT PICTURES.NAME, ALBUMS.NAME from PICTURES INNER JOIN ALBUMS ON PICTURES.ALBUM_ID = ALBUMS.ID ORDER BY ALBUMS.ID;
-- 3 SELECT PICTURES.NAME, USERS.NAME from PICTURES INNER JOIN ALBUMS ON PICTURES.ALBUM_ID = ALBUMS.ID INNER JOIN USERS ON ALBUMS.USER_ID = USERS.ID;